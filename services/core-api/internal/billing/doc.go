// Package billing owns treatments, treatment categories, membership tiers,
// loyalty points and payment orchestration (thin client calling out to the
// payment-service for anything that talks to Xendit).
package billing
