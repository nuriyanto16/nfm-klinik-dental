// Package identity owns users, patients (including family/dependent linking),
// staff, roles and permissions, plus auth (register/login/refresh) and the
// RBAC middleware used by the other domains.
package identity
