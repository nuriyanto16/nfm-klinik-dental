# History

Changelog of updates, additions, and adjustments made to this project.
Newest entries on top. See `docs/architecture.md` for the current-state
design doc and `docs/status.md` for a snapshot of what's done vs. pending.

## 2026-07-30

- **Path API publik diganti dari `/product/klinik/mobile/` ke `/product/klinik/api/`, Flutter Web dikonfirmasi tidak dibangun di VPS.** User memperbarui `VPS.md` sendiri (menghapus baris "Web Flutter") dan menegaskan: "yg flutter jangan dibuild di vps, cukup backend, frontend saja untuk panel admin". Perubahan: location block nginx di-rename (`/product/klinik/api/` → `http://localhost:8092/api/`), `NUXT_PUBLIC_API_BASE` di `.env` VPS diperbarui, `admin-frontend` di-recreate, dan APK dibuild ulang dengan `--dart-define=API_BASE_URL=https://nfmtech.my.id/product/klinik/api/v1`. Diverifikasi: path baru mengembalikan data asli, login penuh (token → dashboard) tetap jalan.

- **Aplikasi mobile dibangun penuh (bukan lagi "coming soon") + di-build APK + deploy seluruh stack ke VPS produksi.** Permintaan user: "Sesuaikan Fungsi dan Tampilan mobile, export apk (kasih saya pathnya)", disusul "deploy ke server" dan "build service mobile di local saja dengan hit endpoint ke vps".
  - **Backend (tambahan kecil untuk mendukung mobile)**: filter `patientId` juga ditambahkan ke `GET /reservations` (selain yang sudah ada di round sebelumnya) dan baru ditambahkan ke `GET /payments` (`ListPayments` sekarang menerima parameter `patientID`) — supaya aplikasi mobile pasien tidak perlu (dan tidak boleh) menarik seluruh data reservasi/pembayaran semua pasien lain hanya untuk memfilter riwayat miliknya sendiri di client (potensi kebocoran PII lintas-pasien kalau ditiru dari pola admin panel apa adanya).
  - **Mobile — identitas tanpa auth asli (sesuai kondisi backend saat ini)**: karena `POST /api/v1/auth/*` untuk pasien belum ada, "Daftar" di mobile berarti membuat baris `identity.patients` baru (relation `self`, tanpa `primaryAccountUserId`) lewat `POST /patients` yang sudah memang mendukung alur ini, lalu menyimpan `patientId` di `flutter_secure_storage` sebagai sesi lokal (`SessionStorage`, `SessionController` — Riverpod `AsyncNotifier`). Halaman login/register lama yang cuma stub UI dihapus, diganti alur "Lengkapi Profil" yang benar-benar memanggil API.
  - **Mobile — layar baru, semua tersambung ke core-api asli**: Home (didesain ulang: kartu sambutan gradient, carousel promo, artikel & tips, testimoni — semua dari `/content/*`), Cabang, Dokter (+ halaman detail dengan jadwal mingguan), Daftar Harga (dikelompokkan per kategori + pencarian), Artikel (list + detail), Reservasi (alur booking bertahap: cabang → dokter → jadwal & keluhan → rencana perawatan dengan estimasi total, `Stepper` bawaan Flutter), Riwayat Reservasi & Riwayat Pembayaran (difilter ke `patientId` sendiri lewat parameter baru di atas), Profil (lihat data diri, logout sesi lokal).
  - **Mobile — infrastruktur pendukung**: `AsyncValueView` (widget generik loading/error/retry untuk semua `AsyncValue`), `SectionHeader`/`EmptyState`, `apiErrorMessage` (ekstrak pesan error backend, mirror `apiErrorMessage` di admin), tema diperkaya (card/chip/navigasi/input theme, bukan cuma warna dasar), lokalisasi Indonesia penuh (`flutter_localizations` + `initializeDateFormatting('id_ID')`) supaya date/time picker berbahasa Indonesia juga.
  - **Bug ditemukan & diperbaiki**: `flutter_local_notifications` butuh core library desugaring di Android Gradle — build APK gagal sebelum ditambahkan `isCoreLibraryDesugaringEnabled = true` + dependency `desugar_jdk_libs`.
  - Diverifikasi: `flutter analyze` bersih (cuma info-level deprecation notice `RadioListTile.groupValue`, bukan error), `flutter test` lulus (widget test disesuaikan ke tampilan Home baru), dan alur penuh dites lewat curl langsung ke core-api yang jalan (buat pasien baru → buat reservasi → riwayat ter-filter benar ke pasien itu saja, riwayat pembayaran kosong sesuai ekspektasi).
  - **Toolchain lokal**: mesin ini belum punya Android SDK/JDK — diinstall (OpenJDK 17 via Homebrew formula, Android command-line tools via cask, platform 35/36 + build-tools) tanpa perlu akses admin/sudo sistem (pakai formula bukan cask untuk JDK, supaya tidak butuh installer `.pkg` yang minta password interaktif).
  - **Push ke GitHub**: repo (sebelumnya belum ada git sama sekali) di-`git init`, `.gitignore` root ditambahkan (mengecualikan `VPS.md` yang berisi kredensial plaintext, `.claude/`, dll — dicek eksplisit sebelum commit agar tidak ada secret ter-commit), lalu di-push ke `https://github.com/nuriyanto16/nfm-klinik-dental.git`.
  - **Deploy ke VPS produksi bersama (shared dengan project lain milik user)**: ditemukan VPS sudah menjalankan ~20 container project lain (satrianusantara, antrian, pos-resto, report) dengan RAM total cuma 3.6GB. Percobaan pertama build 4 image sekaligus (`docker compose up --build`) membuat seluruh VPS **crash & reboot** (kemungkinan besar OOM) — kejadian ini terdeteksi lewat SSH yang tiba-tiba tidak bisa connect sama sekali, lalu setelah VPS kembali `uptime` menunjukkan baru restart beberapa menit. Perbaikan: (1) script deploy baru yang build image **satu per satu** (bukan paralel) dan dijalankan lewat `setsid nohup ... &disown` supaya tidak ikut mati kalau koneksi SSH putus, (2) tambah swapfile 3GB lagi (total jadi ~4.9GB) sebagai buffer, (3) `admin/Dockerfile` dapat `NODE_OPTIONS=--max-old-space-size=3072` karena step Nitro server build sempat crash sendiri dengan "JavaScript heap out of memory" (V8 mendeteksi limit heap default berdasarkan RAM total yang kecil).
  - **Port & reverse proxy**: dipilih port host yang dipastikan tidak bentrok dengan project lain di VPS yang sama (`4001` admin, `8092` core-api, `8093` payment-service, `8094` notification-service, `5435` postgres, `6381` redis — semuanya beda dari port yang sudah dipakai satrianusantara/pos-resto/antrian/report). Override compose baru `docker-compose.vps.yml` (host-port langsung, tanpa Traefik — beda dari `docker-compose.prod.yml` yang asumsinya VPS didedikasikan penuh dengan domain sendiri). Nginx yang sudah ada di VPS (melayani domain `nfmtech.my.id` dengan pola path-per-produk, mis. `/product/satrianusantara/`) ditambahkan 2 location block baru mengikuti pola persis yang sudah ada: `/product/klinik/` → admin-frontend, `/product/klinik/mobile/` → core-api.
  - **Bug: redirect login kehilangan prefix path.** Setelah nginx nyala, `/product/klinik/` redirect ke `https://nfmtech.my.id/login` (bukan `/product/klinik/login`) — Nuxt tidak tahu dirinya di-mount di subpath. Fix: `app.baseURL` baru di `nuxt.config.ts`, overridable lewat `NUXT_APP_BASE_URL` tanpa rebuild. Sempat masih tidak berefek karena env var itu di-set di `.env` tapi **tidak didaftarkan di `environment:` service `admin-frontend`** di `docker-compose.yml` (Compose tidak otomatis meneruskan semua isi `.env` ke dalam container, hanya yang didaftarkan eksplisit) — ditambahkan, baru berfungsi.
  - Diverifikasi end-to-end lewat curl publik sungguhan: `https://nfmtech.my.id/product/klinik/mobile/api/v1/branches` mengembalikan data cabang asli, login lewat `https://nfmtech.my.id/product/klinik/mobile/api/v1/auth/login` mengembalikan token valid, dan token itu dipakai untuk memuat `https://nfmtech.my.id/product/klinik/` yang menampilkan Dashboard sungguhan (bukan redirect ke login lagi). Migrasi & seed data dummy dijalankan di database produksi VPS (bukan cuma lokal).
  - **APK final** dibuild ulang dengan `--dart-define=API_BASE_URL=https://nfmtech.my.id/product/klinik/mobile/api/v1` supaya APK yang didistribusikan langsung bicara ke backend produksi di VPS, bukan `localhost`. Path: `mobile/build/app/outputs/flutter-apk/app-release.apk` (57.3MB).
  - **Belum dikerjakan** (di luar permintaan awal, dicatat sebagai follow-up): Flutter Web build untuk `https://nfmtech.my.id/product/klinik/ninadental` (disebut di `VPS.md` sebagai rencana publish, tapi belum diminta eksplisit dan belum dikerjakan).

- **Login admin panel benar-benar berfungsi (JWT) + skeleton loading di semua menu.** Interupsi user saat melihat halaman login live: "Halaman login perbagus lagi, Login belum berfungsi", disusul "Terapkan skeleton di semua menu, dan panel2 load data yang sifat-nya asyncronous".
  - **Backend — auth domain baru**: `POST /api/v1/auth/login` (email+password, bcrypt compare, hanya untuk staf `role != 'patient'` — pasien tidak pernah punya password asli) dan `GET /api/v1/auth/me` (validasi Bearer token). Token JWT HS256 lewat paket baru `internal/platform/authtoken` (dependency baru `github.com/golang-jwt/jwt/v5`), memakai `JWT_ACCESS_SECRET`/`JWT_ACCESS_TTL_MINUTES` dari config yang sebelumnya sudah di-scaffold tapi belum dipakai. Endpoint lain masih belum ber-RBAC (tetap sesuai catatan "Fase 1" di docs/architecture.md) — ini baru mengaktifkan pintu masuknya, bukan mengunci seluruh API.
  - **Seed**: ditambah 1 user `superadmin` (`admin@ninadentalcare.com`, password `NinaDental#2026`, satu-satunya user dengan bcrypt hash asli — user lain masih placeholder `seed-no-auth-yet` dan sengaja tidak bisa login) supaya ada akun nyata untuk demo/uji login.
  - **Frontend**: composable baru `useAuth.ts` (`login`/`logout`/`isAuthenticated`/`useAuthUser`, token & user disimpan di cookie 30 hari), middleware global baru `middleware/auth.global.ts` (redirect ke `/login` jika belum login, redirect ke `/` jika sudah login tapi buka `/login`). `pages/login.vue` disambungkan ke endpoint asli (pesan error nyata dari backend, bukan lagi stub statis), footer diberi hint kredensial demo. `layouts/auth.vue` diperkaya visual (blob gradient, watermark ikon, 3 bullet fitur dengan ikon) sesuai permintaan "perbagus lagi". `layouts/default.vue` menampilkan nama & role user yang benar-benar login (bukan lagi teks statis "Admin Nina Dental / Superadmin"), dan tombol "Keluar" sekarang benar-benar menghapus sesi. `useApiMutate` (POST/PUT/PATCH/DELETE) mengirim header `Authorization: Bearer` untuk persiapan RBAC mendatang.
  - Diverifikasi end-to-end lewat curl: login sukses mengembalikan token valid, password salah → 401, `/auth/me` tanpa token → 401, request ke `/` tanpa cookie → redirect 302 ke `/login`, request dengan cookie token valid → dashboard ter-render dengan nama user asli.
  - **Skeleton loading**: komponen baru `components/skeleton/{TableSkeleton,StatCardSkeleton,ChartSkeleton,CalendarSkeleton}.vue`. Diterapkan di semua halaman dengan data async — Dashboard (6 KPI + 4 chart + 2 tabel), Reservasi (tabel + kalender), Billing, Pasien, Dokter, Cabang, Perawatan, Inventaris, Rekam Medis, User & Role, CMS (4 tab: Artikel/Promo/Testimoni/Video), Laporan Keuangan (4 KPI + 3 chart) — menggantikan pola lama yang beberapa halaman malah tidak punya indikator loading sama sekali (tabel kosong sekilas sebelum data muncul).
  - Diverifikasi: `nuxi typecheck` bersih, `eslint --fix` bersih, semua 12 halaman utama tetap HTTP 200 setelah rebuild container.

- **Dashboard diaudit ulang untuk memastikan tidak hardcode + diperkaya panel detail per menu + kalender reservasi.** Atas permintaan user: "Dashboard jangan dibuat hardcode, dibuat dinamis dari data dummy", "setiap menu tampilannya dibuat panel nya terlampir data lengkap", "penjadwalan dibuat bagus lagi ada tipe tampilan calendar".
  - **Audit Dashboard**: ditinjau baris demi baris (`pages/index.vue`) — hasilnya sudah 100% dinamis sejak round sebelumnya (6 KPI + 3 chart + 2 tabel semuanya dari endpoint agregat, tidak ada angka tertulis di kode). Masalah sebenarnya bukan hardcode, tapi data dummy terlalu sedikit/sempit sehingga chart tren terlihat datar — diperbaiki lewat penambahan data seed, bukan lewat kode dashboard.
  - **Seed diperkaya**: blok PL/pgSQL baru di `seed.sql` menghasilkan ~40 reservasi + pembayaran historis tambahan tersebar di 30 hari terakhir (status & metode pembayaran bervariasi), supaya chart tren revenue & KPI bulan berjalan terlihat realistis dan tidak datar.
  - **Backend**: `Reservation` sekarang mengekspos `patientId`, `branchId`, `staffId`; `Payment` mengekspos `reservationId`, `patientId` (sebelumnya field FK ini dipakai internal saja tapi tidak pernah dikirim ke API) — dibutuhkan agar frontend bisa mengaitkan riwayat lintas-halaman (mis. reservasi & pembayaran milik satu pasien) tanpa query terpisah per baris.
  - **Frontend — panel detail (mengikuti pola referensi "Kelola Anggota" yang diminta user)**: halaman **Pasien** dan **Dokter** sekarang punya `USlideover` yang terbuka saat baris tabel diklik. Panel Pasien: data pribadi lengkap, riwayat reservasi & riwayat pembayaran pasien tsb (difilter client-side dari list yang sudah di-fetch, berdasarkan `patientId`). Panel Dokter: kartu statistik, data praktik (cabang & spesialisasi), jadwal mingguan, dan reservasi terbaru dokter tsb (difilter berdasarkan `staffId`). Filtering client-side dianggap cukup untuk ukuran data dev saat ini — perlu jadi query server-side (`?patientId=`/`?staffId=` di endpoint `/reservations`) begitu jumlah data membesar.
  - **Frontend — kalender reservasi**: halaman Reservasi dapat toggle **List / Kalender** (`UButtonGroup`). Komponen baru `components/calendar/MonthCalendar.vue` (dibangun dari nol, tanpa library kalender eksternal): grid bulan 6 minggu, navigasi bulan sebelumnya/berikutnya/"Hari Ini", tiap sel tanggal menampilkan hingga 2 chip reservasi berwarna sesuai status (+indikator "N lainnya"), klik tanggal langsung memfilter tampilan List ke tanggal tsb.
  - **Bug ditemukan & diperbaiki (dites lewat curl API langsung, bukan cuma baca kode)**:
    1. **Indexing array 2D PL/pgSQL** — seed sempat pakai `pairs uuid[][] := ARRAY[ARRAY[...], ...]` untuk pasangan cabang/staf; ternyata `pairs[i]` pada array 2D asli di Postgres mengembalikan `NULL` (bukan "baris ke-i" seperti bahasa lain), menyebabkan error `null value in column "branch_id" violates not-null constraint`. Fix: ganti jadi dua array 1D paralel (`branch_ids uuid[]`, `staff_ids uuid[]`) diindeks dengan indeks yang sama.
    2. **Cast enum PL/pgSQL** — variabel `payment_status` bertipe `text` dimasukkan langsung ke kolom bertipe `billing.payment_status` → error `column "status" is of type billing.payment_status but expression is of type text`. Fix: cast eksplisit `payment_status::billing.payment_status` saat insert.
    3. **Kelas Tailwind dinamis tidak ter-generate** — `MonthCalendar.vue` awalnya membentuk nama class lewat template literal (`` `bg-${warna}-100` ``); JIT scanner Tailwind butuh string literal utuh saat build, jadi class hasil template literal tidak pernah masuk CSS akhir (chip status tampil transparan). Fix: peta statis `STATUS_CHIP_CLASS` yang mendaftarkan nama class lengkap untuk tiap 7 status reservasi.
    4. **Cache key `useFetch` bentrok** — halaman Reservasi memanggil `useApiFetch('/reservations')` dua kali (list & kalender) dengan argumen identik; Nuxt men-generate cache key default yang sama untuk keduanya sehingga kedua ref jadi alias satu sama lain (update salah satu menimpa yang lain diam-diam). Fix: `useApiFetch` menerima parameter `key` opsional, dipakai eksplisit (`'reservations-calendar'`) untuk fetch kalender.
  - Diverifikasi: `tsc` bersih, `eslint --fix` bersih, `pnpm run build` sukses, container `admin-frontend`/`core-api` di-rebuild, endpoint `/`, `/patients`, `/doctors`, `/reservations` HTTP 200, dan dikonfirmasi lewat API bahwa `reservations`/`payments` kini membawa field FK baru.

- **Billing dijadikan kasir POS penuh.** Atas permintaan user agar menu billing "diperkaya inputannya seperti POS, dibuat menarik, informatif". Komponen baru `BillingPosCheckout.vue` (modal fullscreen dari tombol "Transaksi Baru (POS)"): katalog perawatan bisa dicari & difilter per kategori (klik kartu untuk menambah ke keranjang), keranjang dengan qty stepper +/- dan hapus baris, input diskon, 4 tombol metode pembayaran bergaya POS (Tunai/QRIS/Transfer Bank/Kartu) dengan ikon, kalkulator "Uang Diterima" → "Kembalian" otomatis untuk tunai, dan layar sukses (receipt) dengan tombol cetak invoice langsung atau mulai transaksi baru. Backend: `CreateReservationInput` dapat field `status` opsional (default `pending`) supaya POS bisa membuat reservasi + langsung menandainya `completed` dalam satu alur (checkout POS = reservasi dan pembayaran dibuat sekaligus, dengan treatment id diulang sesuai qty di keranjang karena `reservation_treatments` tidak punya kolom quantity tersendiri). Diverifikasi end-to-end lewat API: checkout 2x Scaling + 1x Tambal → total Rp950.000 cocok, invoice menampilkan 3 baris item dengan benar. Tombol "Bayar Reservasi" (jalur lama, untuk reservasi yang sudah ada) tetap dipertahankan sebagai opsi terpisah.
- **Ekspansi besar: 6 modul admin diimplementasikan penuh + modul inventaris baru.** Atas permintaan user untuk melengkapi Rekam Medis, Laporan Keuangan, Manage Dokter, Manage Pasien, CMS, User & Role, plus perbaikan Reservasi, transaksi/invoice manual di office, dan modul penggunaan alat/obat dari tindakan pemeriksaan.
  - **Layout & tema**: sidebar dikelompokkan per section (Operasional/Klinis/Keuangan/Konten & Marketing/Sistem), navbar dengan judul halaman dinamis, logo gradient — mengikuti konvensi admin dashboard modern (referensi eksternal yang diminta user tidak bisa diakses — SPA yang butuh login, tanpa tool screenshot/browser automation tersedia).
  - **Login didesain ulang**: layout split-screen dengan panel branding gradient biru, show/hide password, "Ingat saya", "Lupa password?", captcha matematika self-hosted (`useMathCaptcha`).
  - **Migrasi baru** `000008_inventory`: `billing.inventory_items` (alat & obat, kategori, stok, harga satuan, batas reorder) + `clinical.medical_record_items` (pemakaian per tindakan, mengurangi stok otomatis dalam satu transaksi).
  - **Backend**: CRUD penuh untuk dokter (+ cabang praktik & jadwal mingguan), pasien (+ family linking), user & role (staf non-dokter, password bcrypt), reservasi (create + filter + ubah status), pembayaran manual/office + endpoint invoice, inventaris, rekam medis (append-only + odontogram + pemakaian item), CMS (artikel/kategori/promo/testimoni/video), dan laporan keuangan (summary/tren/breakdown periode custom). Paket helper baru `internal/platform/dberr` untuk deteksi FK violation/not-found yang konsisten di semua domain.
  - **Frontend**: 9 halaman baru/upgrade (Dokter, Pasien, User & Role, Reservasi, Billing + halaman cetak invoice terpisah, Rekam Medis, Inventaris, CMS dengan 4 tab, Laporan Keuangan), semua CRUD lewat modal/slideover, composable baru `useApiMutate` (POST/PUT/PATCH/DELETE) mendampingi `useApiFetch` (GET).
  - **Bug ditemukan & diperbaiki saat verifikasi CRUD** (bukan cuma dibaca, dites beneran lewat curl untuk tiap operasi tulis): (1) `CreatePayment` gagal dengan SQLSTATE 42P08 karena parameter dipakai di dua konteks tipe berbeda tanpa cast eksplisit; (2) `CreatePatient` gagal saat menambah anggota keluarga karena `RETURNING` 10 kolom vs `Scan` 9 destinasi (kolom `address` terlewat) — sekaligus ketahuan field `address` tidak pernah diekspos ke API/frontend sama sekali walau diisi di form, jadi ditambahkan penuh (model, semua query, pre-fill form edit).
  - Data dummy ditambah: 8 item inventaris, 3 rekam medis (dengan odontogram & pemakaian item, stok ikut dikurangi), konten CMS contoh (3 kategori + 3 artikel, 2 promo, 3 testimoni, 2 video).

- **Dashboard admin dibuat lengkap & padat.** core-api dapat 4 endpoint agregat baru (`/admin/dashboard/revenue-trend`, `/reservations-by-status`, `/revenue-by-branch`, `/top-treatments`) plus 2 field tambahan di `/admin/dashboard` (`totalPatients`, `revenueThisMonth`). Dashboard sekarang: 6 KPI tile (tadinya 4), 3 chart (revenue 7 hari, reservasi per status, revenue per cabang, perawatan terlaris), 2 tabel data terbaru (reservasi & transaksi). Chart dibangun manual (SVG untuk trend line, HTML/CSS untuk bar chart) mengikuti metode skill dataviz: satu hue per chart (bukan rainbow per-kategori — anti-pattern untuk kategori nominal tanpa urutan alami), direct label, native tooltip via `<title>`, tanpa legend untuk single-series.
- **Login page didesain ulang + captcha ditambahkan.** Layout jadi split-screen (`layouts/auth.vue`: panel branding gradient biru di kiri, form di kanan — sebelumnya cuma card di tengah). Form login dapat: toggle show/hide password, checkbox "Ingat saya", link "Lupa password?", dan captcha matematika self-hosted (`useMathCaptcha` composable, soal "a + b = ?" acak, tombol ganti soal). Captcha divalidasi client-side saja untuk sekarang (belum ada backend auth untuk verifikasi token) — catatan di kode: ganti ke Cloudflare Turnstile (direkomendasikan, gratis) begitu backend siap verifikasi server-side.
- **Admin panel: 7 modul disambungkan ke data nyata.** Dashboard, Reservasi, Pasien, Dokter, Cabang, Perawatan & Harga, dan Billing tidak lagi "coming soon" — sekarang pakai `UTable` dari Nuxt UI, fetch lewat composable baru `useApiFetch` (SSR pakai `core-api:8080` via docker network, client browser pakai `localhost:8080`, lihat `nuxt.config.ts` `runtimeConfig.apiBaseInternal` vs `public.apiBase`). Diverifikasi lewat curl terhadap HTML hasil SSR (nama pasien, status pembayaran, dll. muncul persis sesuai data seed).
- **Bug: timezone Postgres UTC vs WIB.** Ditemukan saat verifikasi — dashboard sempat menunjukkan revenue Rp0 dan reservasi hari ini salah karena container Postgres pakai UTC, sedangkan klinik beroperasi WIB (UTC+7); `CURRENT_DATE` bisa "loncat hari" jam 00:00–06:59 WIB. Fix: migrasi baru `000007_set_timezone` (`ALTER DATABASE nina_dental SET timezone TO 'Asia/Jakarta'`) + restart `core-api` agar connection pool pakai timezone baru + re-seed.
- **Bug: `seed.sql` tidak menghapus `scheduling.branches` sebelum insert ulang** → re-run `make seed` gagal (duplicate key). Fix: tambah `DELETE FROM scheduling.branches` ke bagian cleanup.
- **core-api: read endpoints untuk data admin panel.** Ditambahkan `GET /api/v1/branches`, `/doctors`, `/patients`, `/treatments`, `/reservations`, `/payments`, dan `/api/v1/admin/dashboard` (agregat KPI). Belum ada auth/RBAC di endpoint ini — akan ditambahkan bersamaan dengan domain `auth` di Fase 1.
- **Seed data dummy.** `services/core-api/seed/seed.sql`: 2 cabang (Soreang, Baleendah), 4 dokter, 6 pasien (termasuk 1 anak/family member — Kayla, anak dari Siti Aminah), 4 kategori + 13 jenis perawatan, 10 reservasi (mencakup semua status: pending/confirmed/checked_in/in_progress/completed/cancelled/no_show), 10 transaksi pembayaran dummy (mencakup semua status: pending/paid/expired/failed/refunded), 2 tiket antrian. Idempotent — aman dijalankan berkali-kali (hapus lalu insert ulang baris miliknya sendiri).
- **Bind mount untuk data Postgres & Redis.** Diubah dari Docker named volume ke bind mount (`service-infra-klinik/data/postgres`, `.../data/redis`) — permintaan user agar data tidak hilang saat container di-restart/destroy, dan tidak bisa kehapus tak sengaja lewat `docker volume prune`. Folder `data/` di-`.gitignore`-kan.
- **Rename `infra/` → `service-infra-klinik/`.** Semua referensi path di `Makefile`, `README.md`, `docs/architecture.md`, `docs/status.md` diperbarui. Volume Docker lama (`infra_*`) dihapus karena nama project compose berubah mengikuti nama folder.
- **MinIO dihapus dari stack.** Atas permintaan user — keputusan object storage (untuk foto rontgen/KTP asuransi di Fase 2) ditunda sampai fitur rekam medis benar-benar dibangun, bukan dipasang di awal tanpa kebutuhan konkret.
- **Mulai mencatat perubahan di `HISTORY.md`** — permintaan user agar setiap update/tambahan/adjustment tercatat di sini.

## 2026-07-29 (lanjutan) — Mobile app (Flutter) di-scaffold

- Flutter SDK (3.44.8) diinstall via Homebrew (belum ada di environment sebelumnya).
- `mobile/`: `flutter create --platforms android,ios`, dependencies (`flutter_riverpod`, `go_router`, `dio`, `freezed`/`json_serializable`, `flutter_secure_storage`, `flutter_local_notifications`, dll).
- Struktur `lib/core/{theme,network,router,widgets}` + `lib/features/{home,auth}/presentation`: tema biru Nina Dental Care, home page (quick-menu grid + bottom nav mengikuti alur acuan FDC), halaman login/register (UI saja, belum tersambung ke core-api), 6 route "coming soon" untuk modul yang belum dibangun.
- `riverpod_generator`/`riverpod_lint` sengaja tidak dipasang — konflik versi dependency dengan `json_serializable`/`freezed` di kombinasi Flutter/Dart SDK ini. Pola provider klasik (bukan `@riverpod` annotation) dipakai sebagai gantinya.
- Diverifikasi: `flutter analyze` bersih, `flutter test` lulus.
- CI: `.github/workflows/mobile.yml` ditambahkan (analyze + test; build APK/IPA menyusul, butuh signing secrets & runner macOS/CocoaPods).

## 2026-07-29 — Fase 0 (Foundation) discaffold & diverifikasi end-to-end

- Riset acuan: tinjau screenshot aplikasi FDC Dental Clinic (`BAHAN/MOBILE/`) untuk alur reservasi/pembayaran/profil, dan profil Instagram @ninadental.ndc untuk data nyata klinik (2 cabang Soreang & Baleendah, jam operasional, layanan/Nina Kidz, branding biru/putih).
- Plan arsitektur ditulis & disetujui user (lihat `docs/architecture.md`), termasuk 3 keputusan kunci: payment gateway **Xendit**, notifikasi WA lewat **3rd-party gateway** (Fonnte/Wablas/Qontak), skala **single-tenant multi-branch**.
- Monorepo discaffold: `mobile/`, `admin/`, `services/{core-api,payment-service,notification-service}`, `infra/` (kemudian di-rename), `docs/`.
- **core-api** (Go + Fiber): health check, config/logger/db/redis platform packages, migrasi SQL lengkap (`000001`–`000006`) untuk 5 skema (`identity`, `scheduling`, `clinical`, `billing`, `content`).
- **payment-service** (Go + Fiber): health check, `Provider` interface provider-agnostic (siap diisi implementasi Xendit).
- **notification-service** (Go + Fiber): health check, `WhatsAppGateway`/`PushGateway`/`EmailGateway` interfaces.
- **admin** (Nuxt 3 + Nuxt UI v4 + Tailwind v4): tema biru Nina Dental Care, layout admin (sidebar 12 modul + navbar), layout auth, dashboard KPI placeholder, halaman login (UI saja), 11 halaman modul "coming soon".
- `docker-compose.yml` (base) + `.dev.yml` (port ke localhost) + `.prod.yml` (Traefik + TLS, hanya untuk produksi) + `Makefile` (`make up`, `make migrate-up`, dst.) + `.dockerignore` per service.
- Bug ditemukan & diperbaiki: admin Dockerfile awalnya tidak punya `.dockerignore`, sehingga `COPY . .` menimpa `node_modules` container dengan punya host (macOS) — build gagal di step `pnpm run build`. Fix: tambah `.dockerignore` yang exclude `node_modules`/`.nuxt`/`.output`.
- Diverifikasi end-to-end: `docker compose up` — 7 container sehat, migrasi dijalankan (`make migrate-up`) dan skema+tabel dikonfirmasi via `psql \dt`.
- CI: `.github/workflows/{core-api,payment-service,notification-service,admin}.yml`.
